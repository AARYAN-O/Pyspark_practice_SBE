# Note that it works on each and every row and computes row wise.
# forrach statement does not return anything.
# Note that whenever there is distributed computing , we will be having multiple rows distributed across various worker 
# nodes and hence to handle data across various worker nodes , we use accumulators.

